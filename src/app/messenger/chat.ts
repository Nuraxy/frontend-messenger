import {User} from '../user/user';

export class Chat {
  user!: User;
  friendId!: User;
  chatId!: string;

}
// userId!: number;
// name!: string;
// password?: string;
// email?: string;
// publicKey?: string;
// registerKey?: string;
// confirmed!: boolean;
// newEmail?: string;
// confirmedNewEmail?: boolean;
// userRole!: UserRole;
